sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly
env:
  - TARGET=build
  - TARGET=wasm32-unknown-unknown
matrix:
  allow_failures:
    - rust: beta
    - rust: nightly
script:
  - cargo build --target ${TARGET}
  - cargo test --target ${TARGET}
